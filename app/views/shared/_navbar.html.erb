    <nav class="navbar navbar-expand-lg navbar-light bg-light shadow-sm sticky-top">
      <a class="navbar-brand" href="#">
        <%= link_to image_tag("scubadoo.png", size: "70x70"), root_path %>
      </a>

      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item">
            <%= link_to "Centers", centers_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Trips", trips_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Courses", courses_path, class: "nav-link" %>
          </li>
        </ul>
        <% if user_signed_in? %>
        <div class="nav-item dropdown mx-3">
          <%= image_tag "https://kitt.lewagon.com/placeholder/users/ssaunier", class: "avatar dropdown-toggle", id: "navbarDropdown", data: { toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false %>
          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
            <%= link_to "Edit Profile", edit_user_registration_path, class: "dropdown-item" %>
            <%= link_to "View Bookings", bookings_path, class: "dropdown-item" %>
            <% if current_user.centers.count > 0 %>
              <div class="dropdown-divider"></div>
              <%= link_to "Manage My Centers", manage_centers_path, class: "dropdown-item" %>
            <% end %>
            <div class="dropdown-divider"></div>
            <%= link_to "Logout", destroy_user_session_path , class: "dropdown-item", method: :delete %>
          </div>
        </div>
        <% else %>
          <div class="mx-3">
          <%= link_to "Login", new_user_session_path, class:"btn btn-primary" %>
          </div>
        <% end %>
    </nav>
