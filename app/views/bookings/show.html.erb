<div class="container">

  <div>
  <h1>Your upcoming <%= @booking.listing.category %></h1>
    <div class="card">
      <h5 class="card-header">Booking ID: #<%= @booking.center.id %><%= @booking.id %></h5>
      <div class="card-body">
        <h5 class="card-title"><%= @booking.listing.center.location %> <span class="badge badge-pill badge-success"><%= (@booking.status).upcase %></span></h5>

        <p>No. of diver(s): <%= @booking.no_of_divers %></p>
        <p class="card-text">With supporting text below as a natural lead-in to additional content.</p>
        <table class="table table-bordered">
          <thead>
            <tr>
              <th scope="col">#</th>
              <th scope="col">First</th>
              <th scope="col">Last</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <th scope="row">1</th>
              <td>Mark</td>
              <td>Otto</td>
            </tr>
            <tr>
              <th scope="row">2</th>
              <td>Jacob</td>
              <td>Thornton</td>
            </tr>
          </tbody>
        </table>
  <h3>Listing info</h3>
  <p>Listing: <%= @booking.listing.name %></p>
  <p>Description: <%= @booking.listing.description %></p>
  <p>Date: <%= @booking.listing.date %></p>
  <p>Time: <%= @booking.listing.start_time.strftime("%l:%M %p") %></p>
  <p>Duration per dive: <%= @booking.listing.duration %> minutes</p>
  <p>No. of dives: <%= @booking.listing.dive_count %></p>
  <p>Price: $<%= @booking.costs %></p>

  <h3>Center info</h3>
  <p>Center: <%= @booking.listing.center.name %></p>
  <p>About the center: <%= @booking.listing.center.description %></p>
  <p>Address: <%= @booking.listing.center.address %></p>
  <p>Phone: <%= @booking.listing.center.phone_number %></p>
  <p>Email: <%= @booking.listing.center.email %></p>
      </div>
    </div>
  </div>




</div>
